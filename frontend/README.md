# ΔFusion — Frontend (Next.js)

## Setup
```bash
cd frontend
npm install
```

Backend URL is configured in `.env.local` (defaults to `http://localhost:8000`).

## Run
```bash
npm run dev
```
Open http://localhost:3000

## Build for production
```bash
npm run build && npm start
```
